name: Publish deepforge-keras to npm

on:
  release:
    types:
      - published
  pull_request:
    branches: [master]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Conda
        run: |
          source utils/install-conda.sh

      - uses: actions/setup-node@v1
        with:
          node-version: 12
          registry-url: https://registry.npmjs.org/

      - run: |
          git config --global url."github.com".insteadOf git@github.com:
          git config --global url."git+https://".insteadOf git+ssh://
          npm ci

      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        if: github.event_name == 'publish'
